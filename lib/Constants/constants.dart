import 'package:flutter/material.dart';

const primaryColor = Color(0xFF882C9F);

final iconLinearGradiant = List<Color>.from([
    const Color.fromARGB(255, 251, 2, 197),
    const Color.fromARGB(255, 72, 3, 80)
  ]);